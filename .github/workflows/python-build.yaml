name: "Module: Python Build"
# ------------------------------------------------------------------------------
# Deterministic Python build with pip caching, artifact upload, null-safe outputs.
# ------------------------------------------------------------------------------

on:
  workflow_call:
    inputs:
      working_directory:
        description: "Directory containing requirements.txt"
        type: string
        default: "."
      python_version:
        type: string
        default: "3.11"
      install_command:
        description: "Command to install dependencies"
        type: string
        default: "pip install -r requirements.txt"
      build_command:
        description: "Command to build/package (optional, e.g., 'python -m build')"
        type: string
        default: ""
      artifact_path:
        description: "Directory/File (relative to working_directory) to upload. Leave blank to skip."
        type: string
        default: ""

    outputs:
      artifact_name:
        description: "Name of uploaded artifact (empty if no upload)"
        value: ${{ jobs.build.outputs.artifact_name }}

permissions:
  contents: read
  actions: write        # Required for upload-artifact

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ${{ inputs.working_directory }}
    
    outputs:
      artifact_name: ${{ steps.set_artifact_name.outputs.artifact_name }}

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4.1.1

      # ------------------------------------------------------------------------
      # SETUP & CACHE
      # ------------------------------------------------------------------------
      - name: Setup Python
        uses: actions/setup-python@v5.0.0
        with:
          python-version: ${{ inputs.python_version }}
          cache: 'pip'
          cache-dependency-path: ${{ inputs.working_directory }}/requirements.txt

      # ------------------------------------------------------------------------
      # INSTALL & BUILD
      # ------------------------------------------------------------------------
      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          ${{ inputs.install_command }}

      - name: Build Application
        if: inputs.build_command != ''
        run: ${{ inputs.build_command }}

      # ------------------------------------------------------------------------
      # ARTIFACT UPLOAD (AOT Standard)
      # ------------------------------------------------------------------------
      - name: Upload Build Artifact
        if: inputs.artifact_path != ''
        uses: actions/upload-artifact@v4.3.1
        with:
          name: python-build-${{ github.sha }}
          path: ${{ inputs.working_directory }}/${{ inputs.artifact_path }}
          retention-days: 90

      # ------------------------------------------------------------------------
      # OUTPUT HANDLING (Null Safety)
      # ------------------------------------------------------------------------
      - name: Set Artifact Name Output
        id: set_artifact_name
        run: |
          if [[ -n "${{ inputs.artifact_path }}" ]]; then
            echo "artifact_name=python-build-${{ github.sha }}" >> $GITHUB_OUTPUT
          else
            echo "artifact_name=" >> $GITHUB_OUTPUT
          fi