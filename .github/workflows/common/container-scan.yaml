name: "Container Scan"

on:
  workflow_call:
    inputs:
      image:
        type: string
        required: true
      severity:
        type: string
        required: false
        default: "HIGH"
    outputs:
      scan_report:
        value: "container-scan.json"

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Container scan placeholder"
