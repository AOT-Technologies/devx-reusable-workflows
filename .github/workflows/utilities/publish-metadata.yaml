name: "Publish Metadata"

on:
  workflow_call:
    inputs:
      metadata_json:
        type: string
        required: true

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "Metadata publish placeholder"
