name: "Node CI"

on:
  workflow_call:
    inputs:
      working_directory:
        required: false
        type: string
        default: "."
      config_path:
        required: false
        type: string
        default: "devx.config.yml"
      build_tag:
        required: false
        type: string
    secrets: {}
  
jobs:
  node-ci:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "Node CI workflow will be implemented here."
