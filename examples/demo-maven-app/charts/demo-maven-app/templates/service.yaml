apiVersion: v1
kind: Service
metadata:
  name: {{ include "demo-maven-app.fullname" . }}
  labels:
    {{- include "demo-maven-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "demo-maven-app.selectorLabels" . | nindent 4 }}
