apiVersion: v1
kind: Service
metadata:
  name: {{ include "demo-nodejs-app.fullname" . }}
  labels:
    {{- include "demo-nodejs-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort | default 3000 }}
      protocol: TCP
      name: http
  selector:
    {{- include "demo-nodejs-app.selectorLabels" . | nindent 4 }}
